from setuptools import setup, find_packages

setup(
    name="bhc_modules",
    version="1.0.0",
    description="A collection of modules for the BHC team",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
)
